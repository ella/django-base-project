BEGIN TRANSACTION;
CREATE TABLE "sample_spam" (
    "id" integer NOT NULL PRIMARY KEY,
    "weight" real NOT NULL,
    "expires" datetime NOT NULL,
    "name" varchar(255) NOT NULL,
    "count" integer,
    UNIQUE ("name", "expires")
);
COMMIT;
